# Use an AutoDL PyTorch image whose torch build already supports the instance CUDA.
# Deliberately do not reinstall torch/torchvision here.
numpy>=1.26,<3
Pillow>=10,<13
timm==1.0.28
h5py>=3.10,<4
scipy>=1.11,<2
matplotlib>=3.8,<4
pytest>=8,<10
